# News App

Simple news app to show news headlines

## Getting Started

You'll need a new api key. You can get it from here: https://newsapi.org/register

Create a new file `api.key.dart` in `lib/data/api` like:

```
const kApiKey = '<<< your api key >>>';
```
